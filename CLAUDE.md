# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 仓库概览

这是 FreeBSD Journal（FreeBSD 期刊）的中文翻译项目。基于 GitBook 格式。

源内容为 Markdown 文件，由 GitBook 平台自动构建和部署，无需本地构建步骤。

**翻译基准：** 以 FreeBSD 最新 RELEASE 版本为准，翻译源为 <https://freebsdfoundation.org/our-work/journal/browser-based-edition/> 中列出的对应季度的网页（如果同时提供网页和 PDF，以网页为准，但是网页中代码块内的弯引号 `‘` `’` `“` `”` 存在错误，应该改为直引号 `'` `"`）；网页缺失时以 PDF 兜底。

## 内容架构

### 核心文件

- **`SUMMARY.md`** — 全书唯一数据源，定义完整目录结构和导航树。GitBook 用它来生成侧边栏。 **第一行 `# Table of contents` 绝对不能变更** ，否则 GitBook 同步失效。
- **`CHANGELOG.md`** — 编辑日志，记录翻译进度和同步上游的 commit。
- **`mu-lu.md`** — 目录页，由 `mulu.yml` CI 工作流从 `SUMMARY.md` 自动复制，**不要手动编辑**。
- **`README.md`** — 项目说明。
- **`en/`** — 英文源档案归档目录，按期刊期号分门别类存放英文原文（网页源 Markdown 优先，PDF 兜底），供翻译与校对时逐句对照。

### 目录结构

- 期刊按季度发行，每期为独立章节，目录命名遵循 `<年份>-<月份>/` 模式（如 `2026-010203/`、`2024-0708/`）。
- 每期内每篇文章为独立 `.md` 文件，文件名使用 **拼音 slug**，不得包含空格、中文字符或英文冒号 `:`，必须兼容 Windows 操作系统对文件名的要求（禁止 `con`、`com`、顿号等）。
- `SUMMARY.md` 中每期以 `## <年份-月份> <主题>` 作为章节标题，旗下文章以 `* [标题](路径)` 列出。
- `.gitbook/assets/` — GitBook 静态资源（logo 等）。
- `script/` — 临时脚本与执行日志，无需清理，保持原样。

### 标题管理（关键约束）

`sync-headers.yml` 工作流会在每次 push 时自动将 `SUMMARY.md` 中的标题同步到对应 `.md` 文件的 H1。 **这意味着直接编辑 `.md` 文件的 `# 标题` 会被 CI 覆盖。** 修改章节/文章标题时，只改 `SUMMARY.md` 中对应的 `* [标题](路径)` 条目即可。

## CI/CD 工作流

所有工作流位于 `.github/workflows/`：

| 工作流 | 触发条件 | 说明 |
| ------ | -------- | ---- |
| `sync-headers.yml` | push | 从 SUMMARY.md 同步一级标题到各 .md 文件 |
| `mulu.yml` | SUMMARY.md 变更时 push | 复制 SUMMARY.md → mu-lu.md |
| `markdown-lint2.yml` | workflow_dispatch | markdownlint 检查，规则见 `.github/.markdownlint.json` |
| `md-padding.yml` | workflow_dispatch | 自动在 CJK 与英文/数字间添加空格 |
| `AutoCorrect.yml` | --- | 自动修正常见中文笔误与格式问题 |
| `links.yml` | 定时 + workflow_dispatch | lychee 死链检查，配置见 `.github/lychee.toml` |
| `file-name-check.yml` | --- | 检查 SUMMARY.md 中引用的文件是否存在 |
| `create-pdf.yml` | --- | 导出 PDF |

## 编写规范

### 格式

- **命令行前缀：** `#` 表示 root 权限，`$` 表示普通用户。不要使用 `sudo`。
- **提示块：** tip/important/note/warning/caution 使用 `>` 缩进引用，关键词 **加粗**。
- **代码块：** 无法判断的，再使用 `sh` 作为代码块语言标记兜底，禁止使用 `text` 作为代码块标记，不窜改既有的 `ini` 标记。
- **表格：** 一律居中。
- **禁止 HTML：** 本项目不支持任何 HTML 语法。
- **文件命名：** 使用拼音 slug，文件名中不得包含空格、中文字符或英文冒号 `:`，必须兼容 Windows 操作系统对文件名的要求。
- **路径与 IP：** 全书正文中的路径（带 `/` 或 `\` 的，如 `/etc/rc.conf`、`/usr/local/etc/`）和 IP 地址一律使用 **加粗**，不使用反引号 `` ` `` 包裹；不要混合使用 `*` 和 `` ` ``（如 `*` path `*` 是错误的）。
- **命令、选项、参数：** 命令、选项、可调选项、可调参数等格式使用 `行间代码`（反引号）包裹。注意：不带选项和参数的裸命令，如 mv cp 等，**不加反引号，不进行包裹。**
- **手册页引用格式（重要）：** 禁止将 `vt(4)`、`netmap(4)`、`xz(1)`、`jail(8)`、`make(1)` 等带括号数字的手册页引用改为 `vt.4`、`netmap.4`、`xz.1` 等点号格式，必须保持 `命令(N)` 原始格式。此类引用一律使用 **加粗** 包裹（如 **vt(4)**、**netmap(4)**），不得使用反引号 `` ` `` 包裹，也不得改为点号格式。单个裸命令（如 mv、cp）继续裸着，不加反引号、不加粗。
- **转义字符：** 除非是命令、选项和参数，否则含转义字符 `\` 的元素一律使用 **加粗** 包裹整个元素，不在正文中直接使用转义字符（如写 **PROTO_TYPE** 而非 `PROTO\_TYPE` 在正文里裸露）。逐个手动修改，禁止批量替换。
- **避免滥用 "已" 字：** 如 "XX 已新增" 应改为 "新增 XX"；"已修复" 应改为 "修复完成"。禁止机械替换。
- **禁止篡改：** 不要篡改软件版本号、用户名、带圈数字（如 ①②③ 等），确保其位置、数量和英语原文一致。
- **避免章节交叉引用：** 正文不要出现具体的章节交叉引用（如 "参见第 5 章"），改用语义化链接。
- **代码块注释翻译：** 代码块（` ``` ` 围栏）内的英文注释必须翻译为中文（如 shell 注释 `# This is a comment` → `# 这是一个注释`）。只翻译注释部分，不修改实际命令或代码。保持代码结构和格式不变。
- **fstab 不翻译：** fstab 文件表头（如 `# Device Mountpoint FStype Options Dump Pass#`）及其相关内容保持英文原样，不翻译。
- **标点符号：** 全书正文标点符号统一使用全角，包括引号 `“”` 及括号 `（）` 等，避免半角混用。正文中不允许出现 `「」` 引号，一律替换为 `“”`。
- **链接双冒号：** 不允许使用 `::` 双冒号结尾的链接（如 `mini-memstick::`），必须链接到真实地址。
- **段落与换行：** 每个段落必须在一行内完成，不允许软换行。禁止行尾两个或以上空格创建硬换行、禁止使用 `+` 作为换行符、禁止行尾反斜杠 `\` 创建硬换行。

### 期刊特有规则

- **每期独立目录**：每期对应一个 `<年份-月份>/` 目录，旗下每篇文章独立 `.md`。
- **全部正文翻译**：每期所有正文文章一律翻译，包括但不限于技术文章、**基金会书信（Foundation Letter）、读者来信（We Get Letters / Letters）、日历日程（Calendar）、编辑前言** 等 "非技术正文"，均纳入 `SUMMARY.md`。
- **英文源归档**：翻译与校对前，先将该期英文原文归档至 `en/<年份-月份>-<英文-slug>/`（如 `en/2014-0102-freebsd-10/`），网页源存为 Markdown，弯引号校正为直引号；网页缺失时下载 PDF 兜底。
- **已有标题**：已有标题不宜修改，除非严重扭曲原意。

### 术语

- `Ports` 保持英文不翻译，且保持首字母大写（注意区分真正的 "端口"）禁止机械替换。
- "Jail" 保持英文（不翻译为 "监狱"、"监牢"）禁止机械替换。
- `pkgbase` 保持英文不翻译，禁止译为 "打包基系统" 或类似中文。注意区分："pkgbase" 是项目/工具名，保持英文；"packaged base system"（打包基本系统）是概念，可正常翻译。
- "base system" 统一翻译为 "基本系统"，禁止使用 "基础系统" 或其他变体。禁止机械替换，需逐个确认上下文。
- "The FreeBSD Foundation" 翻译为 "FreeBSD 基金会"，注意自动调整前后空格。代码块（启动信息、git 日志、系统输出）中的 "The FreeBSD Foundation" 不翻译。
- "拷贝" → "复制"，"壳/外壳" → "shell"。禁止机械替换。
- 第二人称一律使用 "你" 而非 "您"
- 所有术语翻译见 `yi-zhe-shuo-ming.md` 对照表
- "Google Summer of Code"（GSoC）三种译法等价：**谷歌 Summer of Code**、**谷歌编程之夏**、**编程之夏**（"Google" 已单独翻译为 "谷歌"，"GSoC" 为英文缩写）。**避免重复**：同一句或同一段中 "谷歌" 只能出现一次，禁止 "谷歌编程之夏（谷歌 Summer of Code）" 这类 "谷歌" 重复形式；首次提及可用 "谷歌 Summer of Code（GSoC）" 或 "谷歌编程之夏（GSoC）" 引入 GSoC 缩写，后续提及择一使用。禁止机械替换，需根据上下文选择形式。

### CJK 空格

中英文、中文与数字之间必须加半角空格。文件和命令名用 `` ` `` 括起来。`md-padding.yml` 和 `AutoCorrect.yml` 工作流会自动检测修复。

### 图片

在正文中插入图片，使用 Markdown 格式。

- **离线目录**：所有图片必须离线至仓库根目录下的 `png/` 目录，按期号分子目录存放，结构为 `png/<期号>/<文章slug>-<序号或描述>.<ext>`（如 `png/2026-010203/events-calendar-01.png`、`png/2024-0708/tcp-01.png`）。
- **禁止远程引用**：正文中不得出现远程 URL 图片引用（`![...](https://...)`），所有图片必须为本地相对路径。
- **引用路径**：从期号子目录（如 `2026-010203/`）下的 `.md` 文件引用上级 `png/` 中的图片时，使用相对路径 `../png/<期号>/<文件名>`（如 `![图示](../png/2026-010203/events-calendar-01.png)`）。
- **文件命名**：`<文章slug>-<序号或描述>.<ext>`，序号按文中出现顺序递增（01、02、…），描述性命名参照原图 URL 末段或上下文（如 `adventures-in-tcp-ip-fig1.png`）。
- **缺失图片强制获取**：远程图片返回 404 或不存在时，必须从对应英文原版网页或 `en/` 目录中的 PDF 提取；若 `en/` 目录无 PDF，从上游 `freebsdfoundation.org/our-work/journal/browser-based-edition/` 下载对应期号 PDF 兜底，再用 PDF 提取工具（如 `pdfimages`、`pdftoppm`、`mutool extract` 等）提取所需图片。所有图片必须存在，不得以 "图片缺失" 占位。
- **图片内容不篡改**：仅下载与路径替换，不修改图片本身内容。
- **禁止批量替换**：每张图片下载与引用替换均需逐个确认。
- **代码块引号**：代码块（围栏 ``` 与行内 ` `）非注释部分（命令、选项、参数、代码、配置、shell 语句、字符串字面量）一律使用直引号 `'` `"`，禁止弯引号 `'` `'` `"` `"`；即使英文原文使用弯引号，也必须校正为直引号。注释部分（已翻译为中文的注释）可保留全角中文标点。

## 欧化汉语规避标准

本标准依据余光中《怎样改进英式中文？——论中文的常态与变态》及现代汉语写作规范制定，作为全部新译与校对的 **强制依据**。逐句校对时按下表逐条排查：

| 序号 | 类别 | 欧化（错误） | 地道（正确） | 说明 |
| ---- | ---- | ------------ | ------------ | ---- |
| 1 | 万能动词+抽象名词 | "作出贡献"、"进行研究"、"予以打击"、"实现改进" | "贡献"、"研究"、"打击"、"改进" | 禁用 "作出/进行/给予/加以/予以/实现" 等万能动词+抽象名词；直接用具体动词 |
| 2 | "一个 XX" 滥用 | "他是一个工程师"、"做一个备份" | "他是工程师"、"备份" | 英语 a/an 不必译 "一个"；表身份、类别一律删 "一个" |
| 3 | "地" 字副词滥用 | "苦心孤诣地想出"、"成功地推翻" | "苦心孤诣，想出"、"推翻" | -ly 不必译 "地"；"成功地" 多余（既做到即成功，删除） |
| 4 | "的" 字堆叠/长定语 | "在 FreeBSD 上运行的用于处理网络流量的工具" | "在 FreeBSD 上运行的工具，用于处理网络流量" | 长定语拆为后置短句；"的" 字不超过两个连用 |
| 5 | 抽象名词做主词 | "他的收入的减少改变了他的生活方式" | "他的收入减少，改变了他的生活方式" | 英文抽象名词做主词自然，中文须还原为短句/动词主语 |
| 6 | 不必要被动句 | "他被升为营长"、"不被准许入学" | "他升为营长"、"未获准入学" | 可用主动处一律改主动；仅施事不明或需突出受事时用被动 |
| 7 | "关于/有关" 滥用 | "关于他的申请，你看过了吗"、"有关交通的问题" | "他的申请你看过了吗"、"交通问题" | "关于/有关" 作介词常多余，删除 |
| 8 | "由于" 滥用 | "由于好奇心的驱使，我向窗内看了一眼" | "出于好奇，我向窗内看了一眼" | 中文因果关系可借上下文不言而喻，少用 "由于" |
| 9 | 连接词 "及/以及" 滥用 | "观念、行动、范围及对象" | "观念、行动、范围、对象" | 中文并列无须连接词；必用时用 "而" 不用 "及" |
| 10 | "们" 字滥用 | "工程师们"、"开发者们" | "工程师"、"开发者" | 慎用 "们"；中文复数靠上下文即可 |
| 11 | "之一" 滥用 | "是最佳方案之一" | "是较佳方案"（视语境） | 慎用 "之一"，非必要不译 one of |
| 12 | 倒装句/后置主语 | "非常重要，这个功能" | "这个功能非常重要" | 主语尽量在前，避免倒装 |
| 13 | 偷换主语 | "打开机器，嗡嗡作响" | "打开机器，机器嗡嗡作响" | 一句之内主语须一致，分句换主语须补出 |
| 14 | "性/型/度" 伪学术词 | "难度很高的技巧"、"热情型的人"、"专业性的字眼" | "很难的技巧"、"热情的人"、"专业字眼" | 禁用 "-性/-型/-度" 等抽象后缀妆点名词 |
| 15 | "在……上/中/下" 滥用 | "在配置过程中" | "配置时" | 介词框架 "在……中" 常冗余，简化 |
| 16 | 重复啰嗦 | "成功地发明了"、"复制品的副本" | "发明了"、"副本" | 删除语义重复的修饰语 |

## 翻译流程

1. 参考官方英文期刊原文进行人工校对
2. 提交 PR 到 main 分支

### 翻译校对工作流程（Claude Code）

对已翻译章节进行质量审校时，参考以下步骤：

1. **获取原文**：通过 `WebFetch` 抓取对应章节的英文原版页面（可将 PDF 下载到 `en/` 文件夹下用于后续处理），获取完整的英文文本。网页源中代码块内的弯引号校正为直引号。

2. **逐句对照**：将中文翻译与英文原文逐句比对，重点检查以下问题类别：

   | 问题类型 | 示例 |
   | -------- | ---- |
   | 事实性错误 | "large parts" 误译为 "三个文件" |
   | 漏译 | 英文原版有但中文缺失的句子 |
   | 机翻腔/表达生硬 | "在阅读本章后，你将会收获" → "通过阅读本章，你将了解" |
   | 用词不当 | "独家优势" 应为 "主要优势"（原文 "particular strengths"） |
   | 版本过时 | 15.0-CURRENT 未同步至 16.0-CURRENT |
   | 语法/文字错误 | 重复字词、"非常地" 应为 "非常" |
   | 欧化汉语 | 详见上文 "欧化汉语规避标准" 16 条 |

3. **提交修改**：
   - 基于 `main` 创建分支 `fix/chapter-X-translation`
   - 仅提交修改的 `.md` 文件，不要包含无关文件
   - 提交信息格式：`fix: 修正第 X 章翻译错误及同步英文原版更新`
   - 用 `gh pr create` 创建 PR，目标分支为 `main`
   - PR 描述中列出每处修改及原因

4. **注意事项**：
   - 不要修改 `# 标题`——会被 `sync-headers.yml` CI 覆盖
   - 保持术语一致性，参考 `yi-zhe-shuo-ming.md`
   - 英文原文可能比翻译版本更新，版本号差异（如 15.0 vs 16.0）应以英文原版为准
   - 添加新内容（如英文原版新增的段落）时，确保翻译风格与上下文一致
   - 禁止篡改带圈数字，如 ①②③ 等，确保其位置、数量和英语原文一致
   - 禁止篡改代码块（包括 shell 命令、配置文件内容、ini 段等），仅修改正文翻译
   - 禁止篡改用户名、邮箱、URL、IP 地址等技术参数
   - 禁止篡改软件版本号
   - 以英文原版期刊 `freebsdfoundation.org/our-work/journal/browser-based-edition/` 为唯一权威源；禁止以中文版已有翻译作为修改依据，避免错误沿袭

5. **逐句校对递归工作流**（核心约束）：
   - **逐行逐句子** 遍历内容，检查逻辑一致性，联网复核后修改
   - **三轮 + 一轮复查**：完整执行三轮逐句校对，然后严格、完整地复查一轮；若仍有问题，继续执行三轮，递归直至无问题
   - **逐个修改**：修改时禁止机械修改、禁止批量；必须逐个手动修改，禁止使用软件工具批量替换或批量逐个复核
   - **修改前后复核**：每次修改前确认问题真实存在、修改后确认改动准确且未引入新错误
   - **不复查旧错**：不得复查以前已经完成三轮的错误；每轮查询内容必须是新的（若与之前的复查重复则跳过、不改，只看是否有新增内容）
   - **联网复查**：对于不存在的实际访问查询（如失效链接、版本号、API 端点），需联网复查三次后修改
   - **禁止绕过审查**：不得以任何方式搜索、批量、绕过逐句子审查流程
   - **允许意译**：直译导致欧化或生硬时，允许意译改写句式，但不得改变技术含义、增删信息、篡改版本号/用户名/带圈数字

## PDF 转换残留内容清理

翻译源为 PDF 兜底时，PDF 转 Markdown 会引入与正文无关的版面元素。清理时遵循以下规范。

### 残留模式

| 模式 | 示例 | 处理 |
| ---- | ---- | ---- |
| 编辑委员会块 | `JOURNAL 编辑委员会`、`## 编辑委员会`、`## 编委会`、`**编辑委员会**`、`## 顾问委员会` + 成员列表 | 删除整块 |
| 出版信息块 | `## 出版信息`、`**出版信息**`、`## 出版团队`、`## 工作人员` | 删除整块 |
| 版权声明 | `版权所有 © 20XX FreeBSD Foundation。保留所有权利。` | 删除 |
| ISBN 信息 | `FreeBSD 期刊（ISBN: 978-0-615-88479-0）每年出版 6 期...` | 删除 |
| 出版地址 | `3980 Broadway St. STE #103-107, Boulder, CO 80304` | 删除 |
| 联系方式 | `电话：720/207-5142 • 传真：720/222-2350`、`邮箱：<info@freebsdfoundation.org>` | 删除 |
| 工作人员列表 | `出版人：Walter Andrzejewski`、`主编：James Maurer`、`美术总监：...` | 删除 |
| 出版商信息 | `S&W Publishing LLC`、`SWP & PUBLISHING LLC`、`发行与出版 LLC` | 删除 |
| 散落字符 | 独占一行的 `TM`、`RE`、`FE` 等 PDF 转换碎片 | 删除 |
| 页码 | 独占一行的纯数字（非代码块内） | 删除 |
| 复制声明 | `未经出版商书面许可，本杂志不得全部或部分复制。` | 删除 |

### 保留内容

以下内容属于正文，**不得删除**：

- **作者署名**（如 `**作者：George Neville-Neil**`）：保留
- **基金会书信正文**：保留
- **书信签名**（如 `George Neville-Neil` / `《FreeBSD 期刊》主编`）：保留
- **正文中的联系邮箱**（如读者来信栏目中 "请联系 Jim Maurer"）：保留
- **正文中的会议地址**（如 `111 Broadway` 餐厅地址）：保留

### 清理方法

1. 用 `Grep` 搜索残留模式（`版权所有 ©`、`ISBN: 978-0-615-88479`、`S&W Publishing`、`## 出版信息`、`## 出版团队`、`## 工作人员`、`P.O. Box 408`、`888/290-9469`、`720/207-5142` 等）
2. 逐个文件读取上下文，确认是 PDF 残留还是正文内容
3. 用 `Edit` 工具精确删除残留块，保留正文内容
4. **禁止批量替换**，必须逐个手动修改
5. 清理后再次 `Grep` 确认无残留
6. 残留块可能在文件开头（版权页）、文末（版权声明）或正文中部（编辑部与正文之间），需全书排查

## 校对与格式统一标准工作流

对已翻译期刊进行全面校对与格式统一时，按以下阶段依次执行。每个阶段均为强制步骤，禁止跳过。

### 阶段 G：逐段校对

1. **获取原文**：通过 `WebFetch` 抓取对应章节的英文原版页面，网页源中代码块内的弯引号校正为直引号；网页缺失时下载 PDF 到 `en/` 文件夹兜底
2. **逐句对照**：将中文翻译与英文原文逐句比对，重点检查事实性错误、漏译、机翻腔、用词不当、版本过时、语法错误、欧化汉语
3. **三轮 + 一轮复查**：完整执行三轮逐句校对，然后严格、完整地复查一轮；若仍有问题，继续执行三轮，递归直至无问题
4. **逐个修改**：修改时禁止机械修改、禁止批量；必须逐个手动修改
5. **联网复查**：对于失效链接、版本号、API 端点等实际访问查询，需联网复查三次后修改
6. **禁止篡改**：不得篡改代码块、用户名、邮箱、URL、IP 地址、软件版本号、带圈数字

### 阶段 H：图片离线

- 检查正文中引用的外部图片 URL，下载到本地 `png/<期号>/` 子目录（不再使用 `.gitbook/assets/`）
- 文件命名遵循 `<文章slug>-<序号或描述>.<ext>` 约定，序号按文中出现顺序递增
- 将远程图片引用替换为本地相对路径 `../png/<期号>/<文件名>`，保留原始图片说明文字（alt）
- 缺失图片强制获取：远程图片返回 404 时，从 `en/` 目录对应期号的 PDF 提取；`en/` 无 PDF 时从上游 `freebsdfoundation.org/our-work/journal/browser-based-edition/` 下载对应期号 PDF 兜底，用 PDF 提取工具提取所需图片；所有图片必须存在，不得以 "图片缺失" 占位
- 同步执行代码块引号校正：代码块非注释部分一律使用直引号 `'` `"`，禁止弯引号 `'` `'` `"` `"`（即使英文原文使用弯引号也必须校正）；注释部分可保留全角中文标点

### 阶段 I：不可见字符与特殊字符清理

- 清理 Unicode 不可见字符：U+200B（零宽空格）、U+200C（零宽非连接符）、U+200D（零宽连接符）、U+2060（单词连接符）、U+FEFF（BOM）、U+200E/U+200F（方向标记）
- 清理 PDF 转换散落字符（独占一行的 `TM`、`RE`、`FE` 等）
- 清理独占一行的页码（非代码块内的纯数字行）
- 逐个手动修改，禁止批量替换

### 阶段 I2：PDF 转换残留内容清理

- 按上文 "PDF 转换残留内容清理" 规范执行
- 搜索并删除编辑委员会列表、顾问委员会列表、出版信息块、版权声明、ISBN 信息、出版地址、联系方式、工作人员列表、出版商信息
- 保留作者署名、正文、签名

### 阶段 J：格式统一

- **路径与 IP**：正文中的路径（带 `/` 或 `\`）和 IP 地址使用 **加粗**，不使用反引号
- **命令、选项、参数**：使用 `行间代码`（反引号）包裹；不带选项和参数的裸命令不加反引号
- **转义字符**：含 `\` 的元素使用 **加粗** 包裹整个元素，不在正文中裸露转义字符
- **标点符号**：全书正文统一使用全角标点（`“”`、`（）` 等）
- **提示块**：tip/important/note/warning/caution 使用 `>` 缩进引用，关键词 **加粗**
- **代码块注释**：代码块内的英文注释翻译为中文，只翻译注释，不修改命令或代码
- **fstab**：fstab 文件表头保持英文原样，不翻译
- **文章结尾结构**：正文文章结尾统一结构为一律先 `---` 再空一行，然后是作者简介；作者人名 **加粗**；没有作者简介的忽略；多个作者之间空一行，各自人名 **加粗**；"此致/此致敬礼" 签名块中，人名也须 **加粗**，且 "此致" 行、人名行、职务行之间各空一行（禁止人名与职务连写不加空行）

### 阶段 K：欧化汉语排查

按 "欧化汉语规避标准" 16 条逐条排查，重点检查：万能动词+抽象名词、"一个 XX" 滥用、"地" 字副词、"的" 字堆叠、抽象名词做主词、不必要被动句、"关于/有关" 滥用、"由于" 滥用、"及/以及" 滥用、"们" 字滥用、"之一" 滥用、倒装句、偷换主语、"性/型/度" 伪学术词、"在……中" 滥用、重复啰嗦。

### 阶段 L：AutoCorrect + md-padding 工具预处理

- 运行 `AutoCorrect` 和 `md-padding` 工具生成 diff
- **逐条人工审核** diff 输出，拒绝不合规范的修改（如错误的全角/半角转换、破坏代码块的建议等）
- 仅接受符合本项目规范的修改，逐个手动应用
- **禁止盲目接受全部 diff**

#### AutoCorrect 审核准则

**应拒绝的常见错误建议：**
1. 全角括号 `（）` → 半角括号 `()`（违反全角标点规范）
2. 全角冒号 `：` → 半角冒号 `:`（违反全角标点规范）
3. 删除 CJK 与英文/引号间空格（违反 CJK 空格规范）
4. 中文连字符前后加空格（如 `编辑-编译-测试` → `编辑 - 编译 - 测试`，中文复合词连字符不需加空格）
5. 代码块内大小写修改（如 `-O json` → `-O JSON`、`-D windows` → `-D Windows`，禁止篡改代码块）
6. `vim` → `VIM`（vim 是正确拼写，不是 VIM）
7. `linux` → `Linux`（需确认上下文，可能是代码标识符如 linux 仿真模块）
8. 中文句号 `。` → 英文句号 `.`
9. 代码块内 `redis` 等用户名/组名改为大写

**可接受的合理建议：**
1. 品牌名称大小写修正：`github` → `GitHub`、`rabbitmq` → `RabbitMQ`（仅限正文，代码块内不修改）
2. 缩写词大小写修正：`json` → `JSON`、`ssl` → `SSL`、`xml` → `XML`、`http` → `HTTP`（仅限正文，代码块内不修改）
3. 程序名保持原样：`http-echo` 等程序名不改大小写，只改正文中独立使用的 `http`

#### md-padding 审核准则

**应拒绝的常见错误建议：**
1. 代码块内的 `=`、`:` 周围加空格（破坏命令、配置、系统输出格式）
2. URL 参数中的 `=` 加空格（如 `?id=xxx` → `?id = xxx`）
3. C++ 命名空间 `::` 加空格（如 `std::vector` → `std:: vector`）
4. Markdown 加粗格式 `**` 加空格（如 `**/path**` → `* */path**`）
5. 美元符号与数字间加空格（如 `$5` → `$ 5`、`$50,000` → `$ 50,000`）
6. 专有名词中的 `*` 加空格（如 `NYC*BUG` → `NYC *BUG`）
7. 命令提示符 `>` 加空格（如 `grub>` → `grub >`）
8. 配置赋值 `=` 加空格（如 `ROOTDEVNAME=/dev/...` → `ROOTDEVNAME =/dev/...`）
9. 系统输出中的 `=`、`:` 加空格（如 `flags=8008<LOOPBACK>` → `flags = 8008 <LOOPBACK>`）
10. 括号内等号说明加空格（如 `host（=从 host 消失）` → `host（= 从 host 消失）`）

**可接受的合理建议：**
1. 参考文献中英文冒号后加空格（如 `issue:December` → `issue: December`）

### 阶段 M：markdownlint 检查与修复

- 运行 `markdownlint-cli2` 检查全部中文 `.md` 文件
- 自动修复可修复的问题（`--fix`）：MD009 行尾空格、MD022 标题周围空行、MD032 列表周围空行、MD034 裸 URL 等
- 手动修复 MD060 表格列风格等无法自动修复的问题
- 确保无 MD056 表格列数错误

#### MD060 表格列风格修复方法

当 markdownlint 报 `MD060/table-column-style [Table pipe does not align with header for option "aligned_delimiter"]` 时：

1. **检查表头列内容宽度**：计算表头每列的字符数（含空格），如 `GPIO SIGNAL` = 11 字符
2. **调整分隔行 dash 数量**：分隔行每列的 dash 数量应使管道符位置与表头行对齐
   - 表头列内容 `GPIO SIGNAL`（11 字符）→ 分隔行应为 `-----------`（11 dash）
   - 表头列内容 `Ball`（4 字符）→ 分隔行应为 `----`（4 dash）
3. **验证管道符位置**：表头行和分隔行的管道符位置必须完全一致
4. **compact 风格**：管道符两侧各保留单个空格，空单元格写作 `| |`

### 阶段 N：更新 CLAUDE.md

- 将本次校对中发现的新问题、新规范标准化写入 CLAUDE.md
- 保持文档结构清晰，便于后续校对参考

### 任务拆分规则

- 每期文章数 ≤ 5 篇时，整期作为一个 Task
- 每期文章数 > 5 篇时，按 3-5 篇子批次拆分
- 每个 Task 独立提交，便于审查和回滚

## Lint 配置

- **markdownlint**（`.github/.markdownlint.json`）：本地已安装 `markdownlint-cli2 v0.22.1`（基于 `markdownlint v0.40.0`），可在本地直接运行检查与修复
  - **本地运行命令**（在仓库根目录执行）：

    ```sh
    # 检查全部中文 .md（排除 en/、.github/、.gitbook/、node_modules/、script/）
    markdownlint-cli2 "**/*.md" "!en/**" "!.github/**" "!.gitbook/**" "!node_modules/**" "!script/**" --config .github/.markdownlint.json

    # 自动修复可修复的问题（MD012 多余空行、MD047 末尾换行、MD034 裸 URL 等）
    markdownlint-cli2 "**/*.md" "!en/**" "!.github/**" "!.gitbook/**" "!node_modules/**" "!script/**" --config .github/.markdownlint.json --fix
    ```

  - **MD060 表格列风格规则（重要）**：配置为 `"style": "compact"` + `"aligned_delimiter": true`
    - **compact 风格**：管道符两侧各保留 **单个空格**，例如 `| cell1 | cell2 |`；空单元格写作 `| |`（不是 `||` 或 `|  |`）
    - **aligned_delimiter**：分隔行 `|---|` 的管道符位置必须与表头行的管道符位置对齐
    - **CJK 显示宽度**：markdownlint 按 **显示宽度** 计算列位置，**每个中文字符按 2 宽度** 计算（与英文 1 宽度不同）。因此含 CJK 的列宽 = CJK 字符数 × 2 + ASCII 字符数 × 1
    - **分隔行 dash 数量公式**：dash 数量应使分隔行管道符位置与表头行对齐
      - **不带冒号**（`---`）：dash 数量 = 列内容显示宽度 W。例如 `GPIO SIGNAL` 显示宽度 = 11，分隔行应为 `-----------`（11 dash）
      - **带冒号**（`:---:`）：dash 数量 = W − 2（每个冒号占 1 宽度）。例如 `FreeBSD 版本` 显示宽度 = 7+1+4 = 12，带冒号分隔行应为 `:----------:`（10 dash）
      - **CJK 显示宽度**：每个中文字符按 2 宽度计算，ASCII 字符按 1 宽度。例如 `版本` = 2+2 = 4 宽度
    - **常见错误修复**：当 markdownlint 报 `MD060/table-column-style [Table pipe does not align with header for option "aligned_delimiter"]` 时，需手动扩展分隔行 dash 数量，使管道符位置与表头按显示宽度对齐；`--fix` 无法自动修复此类问题
  - **MD056 表格列数规则**：表头声明几列，所有数据行都必须有几列；末尾缺空单元格时需补 `| |`，而非省略管道符
- **textlint**（`.textlintrc`）：仅启用 `ja-space-between-half-and-full-width` 规则，用于 CJK/英文空格检查
- **lychee**（`.github/lychee.toml`）：6 线程、30 并发、30 秒超时、最多 3 次重试、Chrome UA、排除私有 IP 和 `ftp.freebsd.org`
